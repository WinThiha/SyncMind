## Overview

This change is a **localization wiring fix only**. No new capabilities are introduced and no existing spec-level requirements are modified.

All functional behavior (filtering, searching, issue creation, detail view) remains unchanged. Only the UI string rendering changes from hardcoded English to translated lookups.

See `proposal.md` and `design.md` for full context.